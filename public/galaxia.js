const canvas=document.getElementById('galaxy');
if(canvas){const ctx=canvas.getContext('2d'),reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let w=0,h=0,raf,last=0;const stars=Array.from({length:100},(_,i)=>({x:(i*.61803398875)%1,y:(i*.41421356)%1,r:.4+(i%4)*.4}));function resize(){w=canvas.clientWidth;h=canvas.clientHeight;canvas.width=w;canvas.height=h;}new ResizeObserver(resize).observe(canvas);function draw(t){if(!document.hidden&&w&&h&&t-last>40){last=t;ctx.clearRect(0,0,w,h);const drift=reduce?0:Math.sin(t*.00006)*w*.08;for(const [x,y,color]of [[w*.24+drift,h*.4,'#594ecb25'],[w*.78-drift,h*.55,'#ca426b20']]){const g=ctx.createRadialGradient(x,y,0,x,y,w*.45);g.addColorStop(0,color);g.addColorStop(1,'transparent');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);}for(let i=0;i<stars.length;i++){const s=stars[i];ctx.globalAlpha=reduce?.5:.35+.25*Math.sin(t*.001+i);ctx.fillStyle=i%3?'#e4ddff':'#ffc798';ctx.beginPath();ctx.arc((s.x*w+(reduce?0:t*.002*(1+i%3)))%w,s.y*h,s.r,0,Math.PI*2);ctx.fill();}ctx.globalAlpha=1;}raf=requestAnimationFrame(draw);}raf=requestAnimationFrame(draw);}
