const canvas=document.getElementById('embers'),ctx=canvas?.getContext('2d');
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
if(ctx){let w=0,h=0,raf;const particles=Array.from({length:36},(_,i)=>({x:(i*.618033)%1,y:(i*.4142)%1,s:.5+(i%3)*.5}));function resize(){w=innerWidth;h=innerHeight;canvas.width=w;canvas.height=h;}resize();addEventListener('resize',resize);function draw(t){ctx.clearRect(0,0,w,h);for(const p of particles){const y=reduce?p.y:((p.y-t*.000009)%1+1)%1;ctx.fillStyle='rgba(244,177,112,'+(.1+Math.sin(y*Math.PI)*.3)+')';ctx.beginPath();ctx.arc(p.x*w,y*h,p.s,0,Math.PI*2);ctx.fill();}if(!reduce&&!document.hidden)raf=requestAnimationFrame(draw);}draw(0);document.addEventListener('visibilitychange',()=>{cancelAnimationFrame(raf);if(!document.hidden)draw(performance.now());});if(!reduce&&matchMedia('(pointer:fine)').matches)addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--drift-x',(e.clientX/w-.5)*12+'px');document.documentElement.style.setProperty('--drift-y',(e.clientY/h-.5)*7+'px');},{passive:true});}
